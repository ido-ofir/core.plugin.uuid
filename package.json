{
  "name": "core.plugin.uuid",
  "version": "0.0.3",
  "description": "Generates a unique id.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ido-ofir",
  "license": "ISC",
  "dependencies": {
    "core.loader.extend": "0.0.1"
  }
}
